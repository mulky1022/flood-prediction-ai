[project]
name = "app"
version = "0.1.0"
dependencies = [ "numpy", "pandas", "scikit-learn==1.6.1", "joblib", "httpx", "fastapi", "uvicorn", "pydantic", "supabase", "python-dotenv" ]
requires-python = "~=3.12.0"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]

